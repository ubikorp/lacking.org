---
// ---------------
// Post Listing ==
// ---------------

import type { Post } from '~/types';

import Item from '~/tlo/widgets/Item.astro';

export interface Props {
  posts: Array<Post>;
}

const { posts } = Astro.props;
---

{posts.map((post) => (
  <Item post={post} />
))}
